using UnityEngine;
using UnityEngine.UI;

public class TransitionSceneController : MonoBehaviour
{
    public Text transitionText; // ?????????UI??
    public static string transitionMessage; // ???????????????

    // ????????????
    void Start()
    {
        // ??????????
        if (!string.IsNullOrEmpty(transitionMessage))
        {
            // ????????UI???
            transitionText.text = transitionMessage;
        }
        else
        {
            Debug.LogWarning("Transition message is empty!");
        }
    }
}
